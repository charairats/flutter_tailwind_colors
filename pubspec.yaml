name: flutter_tailwind_colors
description: Colors and swatchs from Tailwind CSS v3.0 includes 22 primary colors and thier shades.
version: 0.0.1
homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
